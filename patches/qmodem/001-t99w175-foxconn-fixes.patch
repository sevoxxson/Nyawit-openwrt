diff --git a/application/qmodem/files/etc/init.d/qmodem_init b/application/qmodem/files/etc/init.d/qmodem_init
index 4a5a8bf..0e601d0 100755
--- a/application/qmodem/files/etc/init.d/qmodem_init
+++ b/application/qmodem/files/etc/init.d/qmodem_init
@@ -130,0 +131 @@ _try_device()
+    config_get data_interface "$1" data_interface
@@ -141,3 +142,2 @@ _try_device()
-    device_path=`readlink -f $path`
-    is_parent=`echo "$device_path" | grep -q "^$netdevice_path"`
-    if [ -z "$is_parent" ] || [ ! -d "$netdevice_path" ]; then
+    device_path=`readlink -f "$path"`
+    if [ -z "$netdevice_path" ] || [ ! -d "$netdevice_path" ] || ! echo "$netdevice_path" | grep -q "^$device_path"; then
@@ -144,0 +145,21 @@ _try_device()
+        case "$path" in
+            /sys/bus/usb/devices/*)
+                slot_name="${path#/sys/bus/usb/devices/}"
+                slot_name="${slot_name%/}"
+                slot_type="usb"
+                ;;
+            /sys/bus/pci/devices/*)
+                slot_name="${path#/sys/bus/pci/devices/}"
+                slot_name="${slot_name%/}"
+                slot_type="pcie"
+                ;;
+            *)
+                slot_name="${path%/}"
+                slot_type="$data_interface"
+                ;;
+        esac
+
+        [ -z "$slot_type" ] && slot_type="$data_interface"
+        [ -z "$slot_type" ] && slot_type="usb"
+        path="$slot_name"
+        slot="$slot_type"
diff --git a/application/qmodem/files/usr/libexec/rpcd/qmodem b/application/qmodem/files/usr/libexec/rpcd/qmodem
index 621661f..ca96172 100755
--- a/application/qmodem/files/usr/libexec/rpcd/qmodem
+++ b/application/qmodem/files/usr/libexec/rpcd/qmodem
@@ -145 +145 @@ get_at_cfg() {
-        zh*|cn|auto)
+        zh*|cn)
diff --git a/application/qmodem/files/usr/share/qmodem/modem_scan.sh b/application/qmodem/files/usr/share/qmodem/modem_scan.sh
index 0c9e446..de75298 100755
--- a/application/qmodem/files/usr/share/qmodem/modem_scan.sh
+++ b/application/qmodem/files/usr/share/qmodem/modem_scan.sh
@@ -348 +348 @@ match_config()
-    [[ "$name" = *"mv31-w"* ]] || [[ "$name" = *"T99W175"* ]] && name="t99w175"
+    [[ "$name" = *"mv31-w"* ]] || [[ "$name" = *"t99w175"* ]] && name="t99w175"
diff --git a/application/qmodem/files/usr/share/qmodem/vendor/foxconn.sh b/application/qmodem/files/usr/share/qmodem/vendor/foxconn.sh
index 146cb20..f812a0a 100644
--- a/application/qmodem/files/usr/share/qmodem/vendor/foxconn.sh
+++ b/application/qmodem/files/usr/share/qmodem/vendor/foxconn.sh
@@ -56,2 +56,4 @@ function get_mode(){
-    cfg=$(at $at_port $at_pre"PCIEMODE?")
-    config_type=`echo -e "$cfg" | grep -o '[0-9]'`
+    local config_type
+    local driver
+    cfg=$(at "$at_port" "${at_pre}PCIEMODE?")
+    config_type=$(echo -e "$cfg" | awk -F: '/PCIEMODE/ {gsub(/[^0-9]/, "", $2); print $2; exit}')
@@ -60,2 +62 @@ function get_mode(){
-    json_add_int disable_mode_btn 1
-
+        json_add_int disable_mode_btn 1
@@ -63,7 +64,18 @@ function get_mode(){
-          ucfg=$(at $at_port $at_pre"USBSWITCH?")
-          config_type=$(echo "$ucfg" | grep USBSWITCH: |cut -d':' -f2|xargs)
-          if [ "$config_type" = "9025" ]; then
-             mode_num="1"
-          elif [ "$config_type" = "90D5" ]; then
-             mode_num="0"
-        fi
+        ucfg=$(at "$at_port" "${at_pre}USBSWITCH?")
+        config_type=$(echo "$ucfg" | awk -F: '/USBSWITCH/ {gsub(/[ \r]/, "", $2); print toupper($2); exit}')
+        case "$config_type" in
+            *9025*) mode_num="1" ;;
+            *90D5*) mode_num="0" ;;
+        esac
+
+    if [ -z "$mode_num" ]; then
+        driver=$(get_driver)
+        case "$driver" in
+            mbim)
+                mode_num="0"
+                ;;
+            qmi|mhi|rmnet)
+                mode_num="1"
+                ;;
+        esac
+    fi
@@ -117,0 +130,55 @@ set_mode(){
+sim_switch_capabilities(){
+    local response
+    local slots
+
+    response=$(at "$at_port" "${at_pre}SWITCH_SLOT=?")
+    slots=$(echo "$response" | grep -Eio '<[0-9, ]+>' | head -n1 | tr -d '<> \r')
+
+    if echo "$slots" | grep -q '0' && echo "$slots" | grep -q '1'; then
+        json_add_string "supportSwitch" "1"
+        json_add_array "simSlots"
+        json_add_string "" "0"
+        json_add_string "" "1"
+        json_close_array
+    else
+        json_add_string "supportSwitch" "0"
+    fi
+}
+
+get_sim_slot(){
+    local response
+    local sim_slot
+
+    response=$(at "$at_port" "${at_pre}SWITCH_SLOT?")
+
+    if echo "$response" | grep -qi 'SIM1[[:space:]]*ENABLE'; then
+        sim_slot="0"
+    elif echo "$response" | grep -qi 'SIM2[[:space:]]*ENABLE'; then
+        sim_slot="1"
+    else
+        sim_slot=$(echo "$response" | awk -F: '/SWITCH_SLOT/ {gsub(/[^0-9]/, "", $2); print substr($2, 1, 1); exit}')
+    fi
+
+    json_add_string "sim_slot" "$sim_slot"
+}
+
+set_sim_slot(){
+    local sim_slot_param=$1
+    local response
+
+    case "$sim_slot_param" in
+        0|1)
+            ;;
+        2)
+            sim_slot_param="1"
+            ;;
+        *)
+            json_add_string "result" "Invalid SIM slot: $sim_slot_param"
+            return
+            ;;
+    esac
+
+    response=$(at "$at_port" "${at_pre}SWITCH_SLOT=${sim_slot_param}")
+    json_add_string "result" "$response"
+}
+
